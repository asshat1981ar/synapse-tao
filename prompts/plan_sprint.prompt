Given the following backlog:
```json
$BACKLOG_JSON
```

Plan the next sprint for capacity **$CAPACITY** story‑points. Select tasks from the backlog that fit within the capacity. Prioritize tasks with higher priority and lower IDs first.

Return **ONLY** valid JSON in the following format:
{
  "sprint": <int>,
  "tasks": [
    {"id": <int>, "title": <string>, "points": <int>}
  ]
}