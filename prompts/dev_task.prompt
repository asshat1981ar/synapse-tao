For the following task in $REPO:

```json
$TASK_JSON
```

Describe the code changes you would make to complete this task. Include the full paths of any files you would modify, create, or delete. 

Return **ONLY** a natural language description of the changes and a list of affected files. Do NOT include any code or diffs directly.

Example:
"To implement user authentication, I would modify `client/src/components/Auth.jsx` to add login/logout functionality, and `server/routes/auth.js` to handle API requests. I would also create a new file `server/models/User.js` for the user schema."
